
campaigns:
  description: >
    Use the {accountId} and use it to get a list of campaigns.
    ask the human if this is a valid account id.
    
  expected_output: >
    A list of campaign ids for the account as a JSON array. Do not include the whold campaign object. Only the campaign Id.
  


 

  